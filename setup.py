from setuptools import find_packages
from setuptools import setup

setup(
    name="pydiscord",
    version="1.0",
    author="gdroguski",
    description="Yet another discord ripoff in Django, come inside and get wild",
    packages=find_packages(),
    include_package_data=False,
)
